import React from "react";
import { View, ImageBackground, TouchableOpacity } from 'react-native';

const PopularTag = (props) => {
    return(
        <TouchableOpacity>
            <ImageBackground style={{backgroundColor:'#EFEFEF'}}>
                
            </ImageBackground>
        </TouchableOpacity>
        
    )
    
}